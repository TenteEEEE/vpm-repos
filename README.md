# TenteEEEE VPM Packages

This repository is the VPM listing for packages published by TenteEEEE.

The listing source is `source.json`. GitHub repositories listed there are converted into the
VPM-compatible `index.json` by the included GitHub Action after their releases are published.
